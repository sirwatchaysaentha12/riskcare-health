ความถูกต้องของสีและหน้าตาเว็บ (UI/UX Fidelity)

- ตรวจสอบรหัสสีหลัก (Theme) ปุ่มและแถบบาร์ต้องเป็น #0E4B95 [^1]
- ตรวจสอบรหัสสีของการ์ดกลุ่มความเสี่ยง 4 ระดับ (แดง, ส้ม, เหลือง, เขียว) ทั้งพื้นหลังและขอบตัวหนังสือให้ตรงตามที่ดูดสีไว้ [^1]
- ตรวจสอบระยะห่าง (Spacing) และความโค้งมนของกล่องข้อความ (Border Radius) ให้ดูนุ่มนวลตามภาพ [^1]

ความถูกต้องของแบบสอบถามประเมินความเสี่ยง

- [x] แสดงคำถาม 14 ข้อ แบ่งเป็นข้อมูลพื้นฐาน ประวัติสุขภาพ พฤติกรรม/สิ่งแวดล้อม อาการ และความปลอดภัย
- [x] บังคับตอบครบทุกข้อก่อนดูผล และแสดงจำนวนข้อที่ตอบแล้ว
- [x] คำนวณคะแนนต่ำ/ปานกลาง/สูง/สูงมากตาม prototype scoring ใน data.md
- [x] Red flag เปลี่ยนผลเป็นคำเตือนเร่งด่วนโดยไม่รอคะแนน และไม่อ้างว่าเป็นการวินิจฉัย
- [x] มีคำเตือนให้ติดต่อบริการฉุกเฉินเมื่อมีอาการอันตราย
- [x] ปุ่มประเมินถูก disabled จนกว่าจะตอบครบทุกข้อ
- [x] เมื่อกดประเมินหลังตอบครบ มี popup “กำลังประมวลผล” พร้อม loading animation ก่อนแสดงผล
- [x] ระหว่างประมวลผล ปุ่มถูก disabled เพื่อป้องกันการกดซ้ำ

โครงสร้างโค้ดและความสะอาด (Clean Code & Architecture)

- โค้ดต้องไม่มีการเขียนรวมกันเป็นไฟล์เดียวขนาดใหญ่ (Monolithic) [^1]
- ชิ้นส่วนย่อยต้องถูกแยกออกจากกันเป็นระเบียบ (Component-Based) เช่น ปุ่ม, การ์ดแสดงผล, แถบเมนูด้านซ้าย/ด้านบน (Sidebar/Navbar) [^1]
- การส่งค่าผ่านคอมโพเนนต์ (Props) และการควบคุมสถานะ (State Management) ต้องเป็นระบบ [^1]

ความถูกต้องของระบบเปลี่ยนหน้า (Routing & Logic)

- การกดปุ่มเข้าสู่ระบบ, เมนูด้านข้าง, หรือหน้าตั้งค่า ต้องสลับหน้าผ่าน react-router-dom ได้อย่างถูกต้องและลื่นไหล [^1]
- [x] Dashboard ปุ่ม “เริ่มประเมิน” ไปที่ /assessment
- [x] หน้า Assessment มีปุ่มกลับไป /dashboard

ผลการตรวจรอบล่าสุด (1 สิงหาคม 2026)

- `npm run lint`: ผ่าน
- `npm run build`: ผ่าน
- Browser smoke test: 14/14 คำถามแสดงผลและเลือกคำตอบครบได้
- Browser smoke test: ผลคะแนนต่ำแสดงถูกต้อง
- Browser smoke test: red flag แสดงคำเตือน “ต้องได้รับการดูแลเร่งด่วน” ถูกต้อง


ตรวจสอบโครงสร้างโปรเจกต์เว็บไซต์ทั้งหมด โดยมีเป้าหมายเพื่อ จัดระเบียบโค้ดให้เป็นมาตรฐาน อ่านง่าย แก้ไขง่าย และส่งต่อให้ผู้อื่นพัฒนาต่อได้สะดวก โดย ห้ามเปลี่ยนหน้าตา (UI/UX), ห้ามเปลี่ยนการทำงานของระบบ, ห้ามลบฟังก์ชันหรือ Logic ที่จำเป็น และผลลัพธ์ของเว็บไซต์หลังปรับปรุงต้องทำงานเหมือนเดิมทุกประการ (100%)

ให้ตรวจสอบว่าโค้ดแต่ละส่วนทำหน้าที่อะไร อยู่ในหน้าไหน และเกี่ยวข้องกับฟีเจอร์ใด จากนั้นจัดโครงสร้างไฟล์และโฟลเดอร์ให้เป็นหมวดหมู่ที่ชัดเจน เช่น pages, components, layouts, services, utils, hooks, assets, styles, api, config เป็นต้น พร้อมแยกไฟล์ตามหน้าหรือหน้าที่การทำงาน ไม่รวมหลายหน้าหรือหลายฟังก์ชันไว้ในไฟล์เดียวโดยไม่จำเป็น

ให้ตั้งชื่อไฟล์และโฟลเดอร์ให้สื่อความหมายตรงกับหน้าหรือหน้าที่ เช่น Login.html, LoginPage.jsx, Dashboard.jsx, Profile.jsx, Settings.jsx, RiskAssessment.jsx, NotificationService.js, AQIService.js เป็นต้น เพื่อให้ทราบได้ทันทีว่าไฟล์นั้นใช้สำหรับส่วนใดของระบบ

เพิ่ม Comment ในทุกส่วนสำคัญของโค้ด โดยอธิบายสั้น กระชับ และชัดเจน เช่น ส่วนนี้ใช้ทำอะไร ใช้ในหน้าไหน รับผิดชอบฟังก์ชันอะไร หรือเชื่อมต่อกับส่วนใดของระบบ เพื่อให้ผู้พัฒนาคนอื่นสามารถเข้าใจโค้ดได้ทันทีโดยไม่ต้องวิเคราะห์ใหม่

ตรวจสอบโค้ดที่ซ้ำซ้อน โค้ดที่ไม่ได้ถูกเรียกใช้งาน (Dead Code), Import ที่ไม่ถูกใช้, ตัวแปร ฟังก์ชัน Component CSS หรือไฟล์ที่ไม่ได้ใช้งาน รวมถึงโค้ดที่สามารถ Refactor ให้สั้นลงหรืออ่านง่ายขึ้นได้ แล้วลบหรือปรับปรุงเฉพาะส่วนที่ ไม่มีผลต่อการทำงานของระบบ โดยต้องตรวจสอบทุกครั้งว่าหลังแก้ไขแล้วเว็บไซต์ยังทำงานเหมือนเดิมทุกประการ

หากพบไฟล์ที่มีหลายหน้าที่หรือมีโค้ดจำนวนมากเกินไป ให้แยกออกเป็นไฟล์ย่อยตามหน้าที่อย่างเหมาะสม แต่ ไม่ควรแยกจนละเอียดเกินความจำเป็น และไม่สร้างไฟล์เพิ่มโดยไม่มีเหตุผล เพื่อให้โครงสร้างโปรเจกต์ยังคงเรียบง่ายและดูแลรักษาได้ง่าย

ตรวจสอบมาตรฐานการตั้งชื่อไฟล์ ชื่อตัวแปร ชื่อฟังก์ชัน และชื่อ Component ให้ใช้รูปแบบเดียวกันทั้งโปรเจกต์ (Consistent Naming Convention) พร้อมจัดเรียงโค้ด (Formatting) และลำดับของ Import ให้เป็นมาตรฐาน

สุดท้ายให้จัดทำรายงานสรุปว่า มีการย้ายไฟล์อะไร แก้ไขอะไร ลบอะไร และเหตุผลในการแก้ไขแต่ละส่วน พร้อมยืนยันว่าการปรับปรุงทั้งหมด ไม่กระทบต่อการทำงาน หน้าตา และฟังก์ชันของเว็บไซต์ และหากมีส่วนใดที่เสี่ยงต่อการทำให้ระบบผิดพลาด ห้ามแก้ไขหรือลบโดยเด็ดขาด แต่ให้ระบุเป็นข้อเสนอแนะไว้แทน.
เช็ค การเขียน โค้ค เเละการตกเเต่งเว็บไซต์ ในทุกด้าน โดยอิงข้อมูลจากไฟล์ dataprogram 